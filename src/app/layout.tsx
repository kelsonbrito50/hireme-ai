import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "HireMe AI — Smart Job Application Tracker",
  description:
    "AI-powered job application tracker that analyzes job descriptions, matches your skills, and generates tailored cover letters.",
  keywords: ["job tracker", "AI", "cover letter", "career", "skills matching"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
